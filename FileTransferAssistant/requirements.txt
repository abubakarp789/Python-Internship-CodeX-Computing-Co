# Core Dependencies
PySide6>=6.5.0
psutil>=5.9.0
pywin32>=306; sys_platform == 'win32'
watchdog>=3.0.0
send2trash>=1.8.2
pandas>=2.0.0

# Development Dependencies
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
pytest>=7.3.1
pytest-qt>=4.2.0
pytest-cov>=4.0.0

# Optional Dependencies for Additional Features
# Uncomment these if you need the specific functionality
# PyQt5>=5.15.9  # Alternative to PySide6 if needed
# pywin32-ctypes>=0.2.0  # For Windows-specific features
# pywin32>=306; sys_platform == 'win32'  # Windows-specific features
